<%= form_with(model: album_photo, local: true) do |form| %>
  <% if album_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_photo.errors.count, "error") %> prohibited this album_photo from being saved:</h2>

      <ul>
      <% album_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.number_field :album_id %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.text_field :avatar %>
  </div>

  <div class="field">
    <%= form.label :photo_name %>
    <%= form.text_field :photo_name %>
  </div>

  <div class="field">
    <%= form.label :tag_line %>
    <%= form.text_field :tag_line %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
